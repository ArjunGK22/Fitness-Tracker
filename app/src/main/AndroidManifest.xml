<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fitnesstracker">

    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FitnessTracker.NoActionBar">
        <activity android:name=".ReportGeneration"></activity>
        <activity android:name=".ResultScreen" />
        <activity android:name=".MacroCalculator" />
        <activity android:name=".BasicInformation" />
        <activity android:name=".ExtraDetails" />
        <activity android:name=".FitnessInformation" />
        <activity android:name=".ProfileScreen" />
        <activity android:name=".StepsCounter" />
        <activity android:name=".WaterTracker" />
        <activity android:name=".CalorieTracker" />
        <activity android:name=".BodyTracker" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>